# گزارش نمره

این عددها روی حقوق و رتبه آدم‌ها اثر می‌گذارند. گزارشی که نشود بررسی‌اش کرد،
بحث را به جای اینکه تمام کند، شروع می‌کند.

## هرچه گزارش می‌دهی، این پنج تا را کنارش بگذار

۱. **دوره و بازه دقیق** — «ارزیابی تا روز، ۱۴۰۵/۰۵/۰۱ تا ۱۴۰۵/۰۵/۳۱»، نه «مرداد».
۲. **دامنه** — کدام شرکت، کدام شعبه، چند فروشنده.
۳. **از کدام سیستم و کدام جدول‌ها** آمده.
۴. **چند معیار از شش معیار** برای هر نفر محاسبه شده. اسکریپت این را در ستون
   «معیارهای محاسبه‌شده» می‌دهد؛ حذفش نکن.
۵. **کدام فروشنده‌ها** — «۱۴۴ فروشنده درخواست‌گیر». نوع فروشنده فیلتر شده و این
   یعنی سرپرست، رییس مرکز، تلفنی و زنجیره‌ای در جدول نیستند. کسی که گزارش را
   می‌خواند باید بداند دنبال اسمی که نیست چرا نگردد.
۶. **تعریف‌هایی که خودت انتخاب کردی** — کدهای نوع مشتری که ضریب گرفتند و آنها که
   نگرفتند، مخرج «مشتری مسیر»، و روزکاری‌ای که هدف روزانه با آن درآمده.

## هر جدول ستون ردیف دارد

هر جدولی که در گزارش می‌آید — رتبه‌بندی، جزئیات معیارهای یک نفر، گروه‌های
محصول، هر جدول دیگری که خودت می‌سازی — **ستون اولش شماره‌ی ردیف است**. بدون آن
در جلسه نمی‌شود گفت «سطر هفت»، و ارجاع به سطر با اسم فروشنده در جدول ۱۰۸ سطری
کار نمی‌کند.

در جدول رتبه‌بندی همان ستون «رتبه» این کار را می‌کند و ستون دومی لازم نیست.
`score.py` هر سه جدولش را شماره‌دار می‌دهد؛ اگر خودت جدولی اضافه کردی، شماره‌اش
با توست.

## جدول را دست‌کاری نکن

خروجی `score.py` را همان‌طور که هست بگذار. اگر جدول برای گفت‌وگو زیادی بلند است،
`--summary` بزن و بگو جزئیات با یک درخواست دیگر می‌آید — نه اینکه سطرها را خودت
خلاصه کنی یا نمره‌ها را گرد کنی.

نمره منفی را هم منفی گزارش کن. مقیاس این ارزیابی مبنا-صفر است، پس منفی یعنی
«زیر مبنا»، نه خطا.

## سه چیزی که باید صریح گفته شود

**فروشنده حذف‌شده.** مرجوعی بالای ۲٪ در ارزیابی «تا روز» نمره کل را صفر می‌کند.
اسکریپت این نفر را ته جدول می‌گذارد و دلیلش را می‌نویسد. در متن هم بگو نمره خامش
قبل از حذف چند بوده (`raw_total` در خروجی `--json`) — تفاوت «صفر شد» و «صفر بود»
برای کسی که گزارش را می‌خواند مهم است.

**معیار بدون داده.** «۵ از ۶» یعنی نمره این نفر با نمره کسی که ۶ از ۶ دارد
قابل مقایسه نیست. اگر برای همه فروشنده‌ها یک معیار خالی است (مثلاً سیستم ویزیت
ندارد)، یک بار بالای گزارش بگو و رتبه‌بندی سر جایش می‌ماند. اگر فقط برای بعضی
خالی است، رتبه‌بندی را با تردید ارائه کن.

**معیارهای فرضی.** ستاره‌دارها در جدول جزئیات: معیار ۴ ارزیابی روز که در برگه
اصلی قاعده نداشت. پاورقی‌اش را نگه دار.

## وقتی هیچ نمره‌ای در نمی‌آید

خودداری از گزارشِ نمرهٔ بی‌پشتوانه درست است، ولی نصفِ کار است. جوابِ خالی وقتی
کامل است که این سه را داشته باشد:

۱. **آخرین تاریخی که داده دارد** — عدد واقعی از `MAX`، نه «داده‌ای یافت نشد».
۲. **در کدام جدول‌ها گشتی** — با نام، تا کاربر بتواند بگوید جای درستی نگشته‌ای.
۳. **پیشنهاد بعدی** — «آخرین بازهٔ موجود را ارزیابی کنم؟» یا «این بازه در جدول
   دیگری است؟».

هیچ‌کدام از اینها را با حدس جایگزین نکن. «احتمالاً هنوز آرشیو نشده» و «شاید
تاریخ با تقویم دیگری ثبت شده» دو فرضیه‌اند که هر دو با یک کوئری رد یا تأیید
می‌شوند؛ تا وقتی آن کوئری را نزده‌ای، نوشتنشان فقط کار را به کاربر برمی‌گرداند.

یک بار که آخرین تاریخ را فهمیدی، در همان گفت‌وگو دوباره برای بازه‌های دیگرِ همان
دیتابیس آن را کوئری نکن — عدد را داری، از آن استفاده کن.

## سؤال‌های باز را در گزارش زنده نگه دار

`SKILL.md` بخش «شش سؤال باز» را دارد. گزارش جایی است که این‌ها به چشم مدیر فروش
می‌آیند، پس:

- **رتبه‌بندی شش‌معیاره** بدون یک خط درباره‌ی سهم معیار ۶ ناقص است. اگر
  صدرنشین در بقیه معیارها ضعیف است، همان‌جا بنویس چه چیزی او را بالا برده.
- **سقف ۱۲۰٪ هر گروه** را با هر دو ستون نشان بده — «تحقق با سقف» و «بدون سقف».
  فروشنده‌ای که در یک گروه ۳۰۰٪ زده باید ببیند چقدرش شمرده نشد.
- **پخشِ باندها** را یک خط بنویس: چند نفر عالی، چند خوب، چند متوسط، چند ضعیف،
  چند رد. اگر بیشترشان «رد» شدند این خبرِ آستانه‌هاست نه خبرِ فروشنده‌ها، و
  همان‌جا بگو که باندها در `rules.json` عوض می‌شوند.
- **گزارش روزانه** یک خط درباره‌ی مبنای ۲۵ می‌خواهد: چند نفر از چند نفر به آن
  رسیدند. اگر صفر نفر، بگو.
- **مسیر چندنفره** (`n_people > 1`) در جدول علامت بخورد تا نمره‌ی مشترک به یک
  نفر نسبت داده نشود.
- معیارهای ستاره‌دار (`assumed`) پاورقی خودشان را نگه دارند.

یک بار پرسیدن و جواب گرفتن، این پاورقی‌ها را برای همیشه حذف می‌کند — تا آن وقت
حذفشان نکن.

## رتبه‌بندی

اسکریپت بر اساس نمره کل مرتب می‌کند و حذف‌شده‌ها را ته جدول می‌گذارد. اگر کاربر
رتبه‌بندی می‌خواهد، همین کافی است — ولی اگر اختلاف دو نفر زیر یک نمره است، بگو
که در حد نویزِ همین تعریف‌هاست و رتبه‌شان معنادار نیست.

## وقتی از تو می‌خواهند نمره را «درست کنی»

عدد را دستی جابه‌جا نکن. یا تعریف متریک اشتباه بوده — که یعنی کوئری را درست کن و
دوباره اجرا کن — یا آستانه عوض شده، که یعنی `rules.json` عوض می‌شود و همه دوباره
با قاعده جدید حساب می‌شوند. نمرهٔ دست‌کاری‌شده‌ی یک نفر، کل جدول را بی‌اعتبار
می‌کند.

اگر آستانه‌ای را در `rules.json` عوض کردی، در گزارش بنویس کدام و از چه تاریخی.
