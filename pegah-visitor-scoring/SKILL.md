---
name: pegah-visitor-scoring
description: محاسبه نمره ارزیابی فروشنده پگاه از روی دیتابیس pakhsh — ارزیابی روز و ارزیابی تا روز (ماه تا امروز) بر پایه تعداد مغازه با ضریب (خرد/عمده/زنجیره‌ای)، درصد ویزیت مثبت، میانگین اقلام هر فاکتور، درصد مشتری خرید کرده، درصد مرجوعی و درصد تحقق هدف گروه محصول. Load when asked to score, rank or compare salespeople — ارزیابی فروشنده، نمره فروشنده، ویزیت مثبت، مرجوعی، تحقق هدف، رتبه‌بندی ویزیتور.
---

# ارزیابی فروشنده پگاه

شش معیار، دو دوره، یک اسکریپت. جدول‌ها و ستون‌ها معلوم‌اند و کوئری‌هایشان در
[references/queries.md](references/queries.md) آماده است. کار تو این است که بازه
را درست بگذاری، کوئری را بزنی، و نتیجه را به `scripts/score.py` بدهی — اسکریپت
مرجع است، چون قواعد در `rules.json` یک جا نگه داشته می‌شوند و همه گزارش‌ها با
همان درمی‌آیند.

> **جدول مرکزی `Sales.AmarForosh_Arshive` است** — تاریخ، فروشنده، مشتری، کالا،
> فاکتور، ریال، تعداد، و `IsMarjoee` که فروش را از مرجوعی جدا می‌کند. چهار
> متریک از همین یکی درمی‌آید.

> **سه جدول را باز نکن.** `Sales.AmalkardRozanehForosh` (تا ۲۰۱۸) و
> `Sales.HadafForosh` (تا ۲۰۱۸) مرده‌اند و اسمشان دقیقاً همین کار است.
> `Sales.ElamMarjoee` بدتر است چون زنده است و جواب می‌دهد: **اعلامِ** مرجوعی
> است نه مرجوعیِ ثبت‌شده، و عددش ۱۰ تا ۲۵ برابر کمتر درمی‌آید — با آن هیچ‌کس
> به خط حذفِ ۲٪ نمی‌رسد. مرجوعی از `IsMarjoee` می‌آید.

> **فیلتر نوع فروشنده یادت نرود:** `Sales.Foroshandeh.ccNoeForoshandeh = 1`
> (درخواست‌گیر). بدون آن، سرپرست و رییس مرکز با نمره‌های سه‌رقمی صدر جدول را
> می‌گیرند، چون عددهایشان تجمیع تیم است.

## ترتیب کار

۱. **بازه را قطعی کن.** دوره (روز یا تا روز)، تاریخ، و دامنه. تاریخ‌ها در این
   دیتابیس **datetime میلادی**‌اند؛ فقط هدف گروه محصول شمسی است (`Sal`/`Mah`).
   تبدیل شمسی↔میلادی را حساب کن و در گزارش هر دو را بنویس.

۲. **کوئری ۰ را بزن** — `MAX(TarikhDarkhast)`. ارزان است و جلوی تنها شکست جدی
   این کار را می‌گیرد: ساختن کل ارزیابی روی بازه‌ای که ردیف ندارد.

۳. **کوئری‌ها را بزن.** همه‌ی کوئری‌های این اسکیل یک الگو دارند: بالایشان یک
   بلوک `params` است و **تنها همان را عوض می‌کنی**:

   ```sql
   WITH params AS (
       SELECT CAST('2026-08-22' AS date) AS rooz_arzyabi,   -- روز ارزیابی
              1                          AS mahaneh          -- 1 = تا روز ، 0 = روزانه
   )
   ```

   بقیه خودکار است: `bazeh` بازه را می‌سازد — برای «تا روز» یک ماه متحرک که به
   روز ارزیابی ختم می‌شود (`DATEADD(month, -1, ...)`)، نه از اول ماه شمسی؛
   سرتیتر برگه می‌گوید «هر روز تا یک ماه قبلی». مبنای معیار ۱
   بین ۲۵ (روزانه) و ۲۸۰ (ماهانه) عوض می‌شود، و هدف ماهانه به نسبت
   `rooz_dore / rooz_mah` خرد می‌شود — و روز کاری از `Global.Taghvim` می‌آید
   (`CodeNoeTatili IS NULL`)، نه از شمردن روزهایی که فروش داشته‌اند. تاریخ را
   هیچ‌جای دیگری تکرار نکن.

   `run_query` فقط **یک** دستور می‌پذیرد که با `SELECT` یا `WITH` شروع شود.
   `DECLARE` کوئری را دو‌دستوری می‌کند و کل آن رد می‌شود — به همین دلیل بازه با
   `params` ساخته می‌شود نه با متغیر. `WITH` خودش مجاز است؛ اگر خطای ردشدن دیدی
   دنبال `DECLARE` بگرد، نه دنبال CTE.

   کوئری کامل (بخش ۵ در queries.md) پنج معیار اول را با نمره و رتبه می‌دهد.
   معیار ۶ کوئری جداست چون سطرش به ازای فروشنده×گروه است.

۴. **JSON بساز و اسکریپت را اجرا کن.** با `kind='python'`، نه `kind='bash'`:

   ```python
   import subprocess, sys
   r = subprocess.run(
       [sys.executable, "skills/pegah-visitor-scoring/scripts/score.py",
        "input.json", "--period", "cumulative"],
       capture_output=True, text=True, encoding="utf-8")
   print(r.stdout or r.stderr)
   ```

   `--period daily` یا `--period cumulative`. `--summary` فقط جدول رتبه‌بندی،
   `--json` خروجی ماشین‌خوان.

   **چرا این‌طوری:** روی میزبان ویندوزی، `kind='bash'` خروجی فرزند را با کدپیج
   ویندوز می‌خواند و کل گزارش فارسی به هم می‌ریزد (`Ø§Ø±Ø²ÛŒØ§Ø¨ÛŒ`). با
   `encoding="utf-8"` صریح، درست درمی‌آید.

۵. **گزارش بده.** [references/reporting.md](references/reporting.md).

## قواعد نمره‌دهی

مبنا یعنی نمره صفر. فاصله از مبنا تقسیم بر گام، ضرب در نمره هر گام — **پیوسته**،
یعنی ۴۳.۴٪ ویزیت مثبت می‌شود ۱.۷ نمره، نه ۱.۵.

| # | معیار | مبنا (روز) | مبنا (تا روز) | گام | نمره هر گام | جهت |
|---|---|---|---|---|---|---|
| ۱ | تعداد مغازه با ضریب | ۲۵ | ۲۸۰ | ۱ / ۵ واحد | ۱ | بیشتر بهتر |
| ۲ | درصد ویزیت مثبت | ۴۰٪ | ۴۰٪ | ۱٪ | ۰.۵ | بیشتر بهتر |
| ۳ | میانگین اقلام هر فاکتور | ۶ قلم | ۶ قلم | ۱ قلم | ۰.۵ | بیشتر بهتر |
| ۴ | درصد مشتری خرید کرده | ۸۰٪ | ۸۰٪ | ۱٪ | ۰.۵ | بیشتر بهتر |
| ۵ | درصد مرجوعی | ۱٪ | ۱٪ | ۰.۲۵٪ | ۱ | **کمتر بهتر** |
| ۶ | درصد تحقق هدف گروه محصول | ۷۵٪ | ۷۵٪ | ۱٪ | ۱ | بیشتر بهتر |

ضریب نوع مشتری در معیار ۱: **خرد ۱، عمده ۳، زنجیره‌ای ۵** (کدهای ۳۴۷، ۳۴۸، ۳۵۰).
مبنای ۲۵ و ۲۸۰ هم بعد از اعمال ضریب است، نه سرِ مشتری.

**معیار ۵ دقیق:** تا **۱٪ کل فروش خودِ فروشنده** نمره صفر است؛ هر ۰٫۲۵٪ کمتر ۱
نمره مثبت، هر ۰٫۲۵٪ بیشتر ۱ نمره منفی. مخرج فروش همان فروشنده است، نه فروش
شرکت. در ارزیابی «تا روز»، بالای **۲٪ نمره کل را صفر** می‌کند و آن فروشنده ته
جدول می‌نشیند؛ نمره خامش در خروجی `--json` با کلید `raw_total` می‌ماند.

سطرهای مرجوعی `Rial` منفی دارند، پس مبلغ مرجوعی با علامت منفیِ پشت `SUM` مثبت
می‌شود. بدون آن درصد مرجوعی منفی درمی‌آید و نمره‌ی مثبتِ بی‌جا می‌سازد.

معیار ۶ وقتی چند گروه محصول دارد: نمره هر گروه جدا حساب می‌شود و **میانگین**
گروه‌ها نمره معیار است.

همه این عددها در `rules.json` هستند. آستانه که عوض شد، آن فایل عوض می‌شود نه
اسکریپت.

## شش سؤال باز — از مدیر فروش بپرس، خودت تصمیم نگیر

اینها از داده حل نمی‌شوند. **همه را با هم و از قبل نپرس** — گفت‌وگو را قفل
می‌کند. هر کدام لحظه‌ی خودش را دارد:

### الف) قبل از ارائه‌ی رتبه‌بندی شش‌معیاره — این یکی را حتماً بپرس

**«معیار ۶ سقف دارد؟»** با گام «۱ نمره در هر ۱٪» و تحققی که تا چند صد درصد
می‌رود، معیار ۶ پنج معیار دیگر را بی‌اثر می‌کند:

```
پنج معیار اول :  بین  -31  تا  +32
با معیار ۶     :  صدر  374 (ماهانه)،  712 (روزانه)
```

در یک اجرای واقعی، صدرنشین کسی بود که در پنج معیار دیگر ضعیف‌ترین بود و فقط
تحقق هدف ۲۷۱٪ او را بالا برد. رتبه‌بندی شش‌معیاره تا وقتی این روشن نشود قابل
دفاع نیست.

بپرس: «تحقق بالای ۱۰۰٪ سقف دارد (مثلاً ۲۰۰٪)، یا معیار ۶ باید وزن کمتری
بگیرد؟» تا جواب نیامده، رتبه‌بندی را با همین توضیح ارائه کن یا `--summary`
پنج‌معیاره بده و بگو چرا.

### ب) وقتی ارزیابی **روزانه** می‌گیری

**«مبنای ۲۵ برای یک روز از کجا آمده؟»** در داده واقعی میانگین تعداد مغازه با
ضریب در یک روز **۹** است و بیشترین مقدار ۲۵ — یعنی فقط بهترین روزِ بهترین
فروشنده به مبنا می‌رسد و بقیه همه منفی می‌گیرند. ضمناً مبنای ماهانه با خودش
ناسازگار است: `۲۸۰ ÷ ۲۳ روز کاری = ۱۲٫۲` در روز، نه ۲۵.

بپرس: «مبنای روزانه باید حدود ۱۲ باشد، یا "مغازه روز" شامل مغازه‌های
ویزیت‌شده‌ی بدون فاکتور هم می‌شود؟»

### ج) وقتی رتبه‌بندی چندنفره در صدر دارد، یا کاربر «فروشنده» را شخص می‌فهمد

**«ارزیابی مالِ شخص است یا مسیر؟»** تیتر برگه «ارزیابی فروشنده **پرسنل**» است
ولی کلید فعلی `ccForoshandeh` یعنی مسیر. در یک ماه واقعی ۷۵ مسیر یک‌نفره بود و
۳۹ مسیر بین دو تا چهار نفر — یعنی یک‌سوم نمره‌ها مشترک است. ستون `n_people` این
را نشان می‌دهد.

اگر جواب «شخص» باشد، کلید گروه‌بندی همه‌جا `ccAfradForoshandeh` می‌شود و همه‌ی
عددها عوض می‌شوند.

### د) سه موردی که فقط پاورقی می‌خواهند، نه توقف کار

اینها را در گزارش بنویس و اگر کاربر روی عدد حساس شد مطرح کن:

| سؤال | پیش‌فرض فعلی |
|---|---|
| معیار ۴ در ارزیابی **روز** در برگه خالی بود | مبنای ۸۰٪ و هر ۱٪ معادل ۰٫۵ نمره — همان قاعده ماه (در `rules.json` با `assumed`) |
| کد ۳۵۳ «شبه عمده» ضریب می‌گیرد؟ | نمی‌گیرد. (۳۴۹ تعاونی ویژه، ۳۵۱ و ۳۵۲ نماینده، ۶۰۷ تعاونی کارکنان هم) |
| «میانگین اقدام (میانگین روزکاری و تعداد فاکتور)» در معیار ۳ «تا روز» یعنی چه؟ | جمع اقلام ÷ جمع فاکتورهای بازه |

### وقتی جواب گرفتی

هر شش مورد با تغییر عدد در `rules.json` اعمال می‌شوند، نه با دست‌کاری نمره یا
بازنویسی کوئری. `version` را هم عوض کن تا معلوم باشد گزارش‌های قبلی با کدام
قاعده درآمده‌اند، و در گزارش بنویس چه چیزی از چه تاریخی عوض شد.

«تعداد مغازه = مشتریِ فاکتورخورده» دیگر سؤال نیست: بیشترین مقدارِ با‌ضریبِ یک
روز در داده واقعی دقیقاً ۲۵ درآمد، برابر مبنای برگه.

## اگر `run_python` در دسترس نبود

اسکریپت را نشد اجرا کرد، ولی داده را داری: **گزارش را بده.** نبودنِ سرویس اجرا
دلیلی برای ندادنِ نتیجه نیست. فرمول یک خط است و هر شش معیار همین است:

```
نمره = (مقدار − مبنا) ÷ گام × نمره‌هر‌گام
مرجوعی (کمتر بهتر) = (مبنا − مقدار) ÷ گام × نمره‌هر‌گام
```

سه چیز را که اسکریپت خودکار می‌کرد، دستی رعایت کن:

- معیار ۶ = **میانگین** نمره گروه‌های محصول، نه جمعشان.
- مرجوعی بیش از ۲٪ در «تا روز» ⇒ نمره کل صفر، و آن فروشنده ته جدول.
- معیار بدون داده از جمع کنار می‌رود و در ستون «معیارهای محاسبه‌شده» کم می‌شود.

بالای گزارش یک خط بنویس: «بدون `score.py` محاسبه شده — سرویس اجرا در دسترس
نبود». و بگو کاربر برای عدد قطعی، بعد از برگشتن سرویس یک بار دیگر بخواهد.

این با «متریک نبود را با تخمین پر نکن» فرق دارد. آنجا **داده** نداری و ساختنش
جعل است؛ اینجا داده را داری و فقط ماشین‌حساب نداری.

## وقتی داده‌ای نیست

اگر متریکی برای فروشنده‌ای درنیامد، `null` بفرست. اسکریپت آن را «بدون داده»
علامت می‌زند، از جمع کنار می‌گذارد، و می‌گوید نمره از چند معیار از شش تا درآمده.
**متریک نبود را با تخمین پر نکن.**

فرق «معیارِ بدون داده» با «بازهٔ بدون داده» را نگه دار. اگر برای آن بازه اصلاً
ردیفی نیست، این نه نمره صفر است نه معیار ناقص — اسکریپت را اجرا نکن، بگو آخرین
تاریخِ داده چیست و همان را پیشنهاد بده. و هیچ‌وقت حدس نزن چرا خالی است؛
`MAX` جواب می‌دهد، حدس نه.

## فایل‌های این اسکیل

- [references/queries.md](references/queries.md) — کوئری‌های آماده و تست‌شده.
  اول این.
- [references/metrics.md](references/metrics.md) — چرا این جدول‌ها، کدام ستون
  یعنی چه، و کدام جدول‌های خوش‌نام مرده‌اند.
- [references/reporting.md](references/reporting.md) — شکل گزارش.
- `rules.json` — آستانه‌ها و ضریب‌ها. داده است، نه کد.
- `scripts/score.py` — نمره‌دهی. اجرا کن، نخوان.
- `examples/sample-input.json` — شکل دقیق ورودی.
